# Go REST API

Простой RESTful сервер на Go с использованием современных практик и инструментов.

---

## 🚀 Технологии

- **Golang** – основной язык
- **Chi** – HTTP роутер
- **Slog** – структурированное логирование
- **Goose** – миграции базы данных
- **Cleanenv** – управление конфигурацией через `.env`
- **Postgres** – база данных
- **Docker** – контейнеризация

---

## 📦 Сборка проекта

Собрать образы Docker (без использования кеша):

```bash
docker compose build --no-cache
```

Запуск:

```bash
docker compose up 
```

## 🛠 Проверка работы

Проверка:

После запуска сервиса, проверить endpoint:

```bash
curl http://localhost:8080/api/v1/users
```


## 📂 Структура проекта

.
├── cmd/server        # Точка входа приложения
├── internal/
│   ├── config        # Конфигурация приложения
│   ├── domain        # Доменные модели
│   ├── repository    # Репозитории работы с БД
│   ├── service       # Сервисный слой (бизнес-логика)
│   ├── transport     # HTTP хендлеры и маршруты
│   └── storage       # Инициализация БД и миграции
├── migrations/       # SQL миграции Goose
├── Dockerfile
├── docker-compose.yml
└── .env


------------------------------------------------------------

Что дальше (следующие шаги):

2. Context timeouts

4. Graceful shutdown

5. CRUD операции

Dependency injection

OpenAPI / Swagger

Тесты (unit + integration)